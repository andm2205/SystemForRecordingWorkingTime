@model SystemForRecordingWorkingTime.Models.RequestTypeChooser

@{
    ViewData["Title"] = "CreateRequest";
}

<h1>CreateRequest</h1>

<h4>Request</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateRequest1">
            <div class="form-group">
                <label class="control-label">RequestType</label>
                <select asp-for="TypeIndex" asp-items="@ViewBag.RequestTypeList" class="form-control"></select>
            </div>
             <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" />
            </div>
        </form>
        @if (ViewBag.PartialViewName != null)
        {
            Html.RenderPartial((string)ViewBag.PartialViewName);
        }
    </div>
</div>

<div>
    <a asp-action="RequestList">Back to List</a>
</div>

